{
  "name": "solo11come-root",
  "version": "1.0.0",
  "engines": {
    "node": ">=14.0.0"
  },
  "scripts": {
    "install-backend": "cd backend-node && npm install",
    "build": "npm run install-backend",
    "start": "cd backend-node && node server.js"
  }
}
